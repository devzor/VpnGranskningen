services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: vpngranskningen
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: devpassword
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./backend/VpnGranskningen.Infrastructure/Persistence/Schema/000_init.sql:/docker-entrypoint-initdb.d/000_init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d vpngranskningen"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
